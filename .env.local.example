# OpenAI API Key
# For local development, use VITE_OPENAI_API_KEY (accessible in vite dev plugin)
# For Vercel production, set OPENAI_API_KEY in the Vercel dashboard
VITE_OPENAI_API_KEY=sk-proj-...

# Simple password protection (no database required)
# Set this to enable password protection for the app
APP_PASSWORD=your-secure-password-here

# Vercel KV (auto-provided when KV is linked in Vercel dashboard)
# For local development, you can use Vercel KV's local development mode:
# Run `vercel env pull` to sync env vars, or use `vercel dev` for local KV
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
